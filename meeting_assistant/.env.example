# ── Meeting Assistant — Environment Variables ──────────────────────────────────
#
# Copy this file to `.env` and fill in your values:
#   cp .env.example .env
#
# The app loads this automatically via python-dotenv.
# NEVER commit your real .env file — it's in .gitignore.
# ──────────────────────────────────────────────────────────────────────────────

# Required: Your Google Gemini API key (free — no credit card needed)
# Get one at https://aistudio.google.com → "Get API key"
GEMINI_API_KEY=AIzaSyDBa
